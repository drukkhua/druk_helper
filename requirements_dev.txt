
# Дополнительные async инструменты для разработки
aiofiles>=23.0.0
aiohttp>=3.8.0

# Безопасность
bandit>=1.7.5

# Development dependencies - не нужны на продакшене
# Линтеры и форматирование
black>=23.0.0
coverage>=7.0.0
flake8>=6.0.0
# isort>=5.12.0  # Убран из проекта - слишком капризный

# Профилирование и отладка
memory-profiler>=0.60.0
# mypy>=1.0.0  # Убран из проекта - слишком медленный

# Pre-commit hooks
pre-commit>=3.0.0
psutil>=5.9.0

# Тестирование
pytest>=7.2.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-mock>=3.12.0
safety>=3.0.0
