# Telegram Bot "Яскравий друк" v2.0 🚀

Бот-помощник для быстрых ответов клиентам с готовыми шаблонами по всем категориям товаров.

## ✨ Возможности бота

### 🎯 Основной функционал:
- **📇 Визитки** - готовые ответы по визиткам
- **👕 Футболки** - шаблоны для печати на текстиле  
- **📄 Листовки** - ответы по полиграфической продукции
- **🔍 Поиск** по ключевым словам по всем категориям
- **🇺🇦/🇷🇺 Двуязычность** - переключение украинский/русский
- **📋 Удобное копирование** - текст в отдельном сообщении

### 📊 Статистика и аналитика:
- **JSON статистика** использования шаблонов
- **Админ-панель** с кнопкой "📊 Статистика" 
- **Логирование** в файл и консоль
- **Анализ популярности** шаблонов

### 🔧 Техническое:
- **Сохранение состояния** меню при копировании
- **CSV формат** для легкого редактирования шаблонов
- **Разделитель ";"** для поддержки запятых в текстах

## 🚀 Быстрый старт

### 1. Создание бота в Telegram

1. Найдите бота [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Задайте имя бота (например: "Яскравий друк Helper")
4. Задайте username бота (например: "yaskravyi_druk_bot")
5. Скопируйте токен бота

### 2. Установка и настройка

```bash
# Создаем папку проекта
mkdir telegram-bot-yaskravyi
cd telegram-bot-yaskravyi

# Устанавливаем зависимости
pip install aiogram==3.4.1 python-dotenv==1.0.0

# Создаем .env файл
touch .env
```

### 3. Настройка .env файла

Создайте файл `.env` с вашими данными:

```env
# Telegram Bot Configuration
BOT_TOKEN=ваш_токен_от_botfather
BOT_NAME=YaskravyiDrukBot

# Admin Settings (ваши Telegram ID через запятую)
ADMIN_USER_IDS=123456789,987654321
COMPANY_NAME=Яскравий друк

# Links
PORTFOLIO_LINK=https://t.me/druk_portfolio

# Development
DEBUG=True
LOG_LEVEL=INFO
```

**Как узнать свой Telegram ID:**
- Напишите боту [@userinfobot](https://t.me/userinfobot)
- Он пришлет ваш ID

### 4. Подготовка данных

Создайте структуру папок и файлов:

```
telegram-bot/
├── bot.py                    # Основной код бота
├── .env                      # Переменные окружения
├── requirements.txt          # Зависимости
├── README.md                # Эта инструкция  
└── data/                    # Папка с данными
    ├── visitki_templates.csv    # Шаблоны визиток
    ├── futbolki_templates.csv   # Шаблоны футболок
    ├── listovki_templates.csv   # Шаблоны листовок
    └── stats.json              # Статистика (создается автоматически)
```

### 5. Формат CSV файлов

Все CSV файлы должны иметь структуру с разделителем `;`:

```csv
category;subcategory;button_text;keywords;answer_ukr;answer_rus;sort_order
визитки;новый_клиент_цена;💰 Новый клиент - цена;цена,стоимость,сколько стоят;Український текст відповіді;Русский текст ответа;1
футболки;новый_клиент_цена;💰 Новый клиент - цена;цена,футболки,печать;Український текст;Русский текст;1
```

**Важно:** 
- Разделитель полей: `;` (точка с запятой)
- В текстах можно использовать запятые
- `sort_order` - порядок отображения кнопок

### 6. Запуск

```bash
python bot.py
```

## 📋 Функционал в деталях

### 🎯 Пользовательский сценарий:
1. **Старт** - `/start` показывает главное меню
2. **Выбор категории** - Визитки/Футболки/Листовки
3. **Выбор шаблона** - по типу запроса клиента
4. **Просмотр ответа** - готовый текст + кнопки
5. **Копирование** - кнопка "📋 Копировать" → текст в отдельном сообщении
6. **Продолжение работы** - меню восстанавливается в той же категории

### 🔍 Поиск:
- Работает по всем категориям одновременно
- Ищет в `keywords` и `button_text`
- Показывает до 10 результатов
- Минимум 2 символа для поиска

### 🇺🇦🇷🇺 Языки:
- Переключение кнопкой в главном меню
- Сохраняется для каждого пользователя
- Влияет на интерфейс и тексты ответов

### 📊 Статистика (только для админов):
- **Кнопка "📊 Статистика"** в главном меню
- **Команда `/stats`** в чате
- Показывает за последние 7 дней:
  - Количество просмотров по категориям
  - Количество копирований
  - Время последнего использования

## 📁 Структура JSON статистики

Файл `./data/stats.json` автоматически ведет статистику:

```json
{
  "2025-07-01": {
    "визитки": {
      "1": {
        "count": 15,
        "last_used": "14:30:22", 
        "copies": 8
      },
      "2": {
        "count": 8,
        "last_used": "13:45:11",
        "copies": 3
      }
    },
    "футболки": {
      "1": {
        "count": 5,
        "last_used": "12:15:33",
        "copies": 2
      }
    }
  }
}
```

## 🔧 Управление шаблонами

### Добавление нового шаблона:

1. Откройте нужный CSV файл (например, `visitki_templates.csv`)
2. Добавьте новую строку:

```csv
визитки;премиум_услуги;✨ Премиум услуги;премиум,дорого,люкс;Преміум послуги доступні;Премиум услуги доступны;10
```

3. Перезапустите бота
4. Новый шаблон появится в категории

### Редактирование существующего:

1. Найдите строку в CSV файле
2. Измените нужные поля
3. Сохраните файл
4. Перезапустите бота

## 🚨 Решение проблем

### Бот не отвечает:
- Проверьте правильность токена в `.env`
- Убедитесь что бот не заблокирован в Telegram
- Проверьте логи в файле `bot.log`

### Шаблоны не загружаются:
- Проверьте формат CSV файлов (разделитель `;`)
- Убедитесь что файлы в кодировке UTF-8
- Проверьте логи - там будет указана причина

### Кнопка не работает:
- Проверьте `subcategory` в CSV - должна быть уникальной
- Убедитесь что нет пробелов в начале/конце полей
- Перезапустите бота после изменений в CSV

### Статистика не записывается:
- Проверьте права на запись в папку `./data/`
- Убедитесь что папка существует
- Проверьте логи на ошибки

## 📊 Логи и мониторинг

### Файлы логов:
- **`bot.log`** - общие логи работы бота
- **`./data/stats.json`** - статистика использования
- **Консоль** - текущая активность в реальном времени

### Полезные команды для админов:
- `/start` - перезапуск главного меню
- `/stats` - просмотр статистики

## ⚡ Развертывание на сервере

### Для продакшена рекомендуется:

1. **VPS/сервер** с Python 3.8+
2. **Systemd service** для автозапуска
3. **Логирование** в отдельные файлы
4. **Backup** CSV файлов и статистики

### Пример systemd service:

```ini
[Unit]
Description=Yaskravyi Druk Telegram Bot
After=network.target

[Service]
Type=simple
User=bot
WorkingDirectory=/path/to/bot
ExecStart=/usr/bin/python3 bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### Установка service:
```bash
sudo cp yaskravyi-bot.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable yaskravyi-bot
sudo systemctl start yaskravyi-bot
```

## 🔮 Планы развития

### В разработке:
- **Web-панель** для управления шаблонами
- **Telegram Business API** интеграция
- **Экспорт статистики** в Excel
- **Уведомления** админам о популярных запросах
- **A/B тестирование** шаблонов

### Возможные улучшения:
- **Голосовые** ответы для мобильных пользователей
- **Изображения** в шаблонах
- **Быстрые ответы** на основе контекста
- **CRM интеграция**

## 📞 Поддержка

При возникновении проблем:

1. **Проверьте логи** в `bot.log`
2. **Убедитесь в правильности** `.env` файла  
3. **Проверьте формат** CSV файлов
4. **Перезапустите бота** после изменений

## 📝 Лицензия

Проект создан для внутреннего использования компании "Яскравий друк".

---

**Версия:** 2.0  
**Дата обновления:** Июль 2025  
**Статус:** ✅ Готов к продуктивному использованию